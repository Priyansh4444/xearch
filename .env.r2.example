# Copy to .env.r2 (gitignored). Used by `pnpm collect:sync` (apps/collector/scripts/sync-runs.sh).
# Cloudflare dashboard > R2 > Manage R2 API Tokens > Create: Object Read & Write, bucket xearch-runs.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=xearch-runs
